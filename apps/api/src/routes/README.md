# API Routes

Route plugins will be added here as REST surfaces are implemented. Public journey routes, insurer dashboard routes, reviewer routes, and admin routes should remain explicit and separately authorized.
