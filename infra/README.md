# Local Infrastructure

Local-only infrastructure helpers live here. The Docker Compose file provides PostgreSQL 17 using the required database and user names, with the password supplied from the environment.
